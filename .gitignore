# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Lockfiles (track one; here we track npm)
package-lock.json
# If using yarn/pnpm instead, unignore their lockfile and ignore others:
#!yarn.lock
#!pnpm-lock.yaml

# Environment / config
.env
.env.*
.gatorconfig.json

# TypeScript
dist/
build/
*.tsbuildinfo

# Drizzle
# Keep migrations (SQL and meta) tracked
drizzle/
.drizzle/
.drizzle-kit/

# SQLite databases (local only)
*.db
*.db-journal
src/**/db.sqlite
src/**/migrations/*.sqlite

# OS / Editors
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Tool caches / builds (optional but common)
.turbo/
.parcel-cache/
.vite/
.next/

# Coverage / test artifacts
coverage/
.nyc_output/
.eslintcache

# Logs
logs/
*.log
*.log.*